#!/bin/bash
case "$1" in
  post)
    # 后台执行，避免阻塞系统唤醒流程
    (
      sleep 5
      if ! ip link show singbox_tun &>/dev/null; then
        logger -t sing-box-resume "TUN interface missing after resume, restarting"
        systemctl restart sing-box
      fi
    ) & 
    ;;  
esac
